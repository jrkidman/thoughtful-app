# todo
- backend: mostly done

- frontend: 
    - build prompt input page, get functional
    - change count of random prompt getter to be able to account for added prompts
    - work on checkboxes, why aren't they working?  then modify user selections and modify prompt api calls to only get selected categories
    -admin page would be nice to be able to make changes to users, add prompts
    -still need edit profile page to work.  how do i target the logged in user and edit their profile?